# 待辦筆記：工具參數的「抄寫失真」問題

狀態：**已觀察到，尚未修，等更多測試樣本確認規律。**

## 現象

用右鍵選單「真的假的」查核選取文字時，代理呼叫 `open_new_tab` 開搜尋引擎，
網址裡的搜尋關鍵字跟原文對不上，字被改掉了。

實例：

| 原文（使用者選取） | 實際送去搜尋的關鍵字 |
| --- | --- |
| 羅馬混凝土 | 羅馬元紐土 |

結果是拿一個不存在的詞去搜，查核從第一步就歪掉。

## 判斷的根因

目前架構是：選取文字 → 塞進 prompt → **LLM 自己重新生成一次那串字**
→ 當成 `open_new_tab` 的 `url` 參數 → 開搜尋頁。

中間那步是「重打」不是「複製」。模型逐 token 生成，中文字被拆成多個 token，
小模型複製字串時容易掉字或換成形近／音近的字。**不需要誤解語意也會出這種錯。**

也就是說，這是**抄寫類錯誤**，而抄寫本來就不該交給模型做——原文我們手上有完整一份。

次要因素：網址若沒有 `encodeURIComponent`，中文可能再糊一層（非主因）。

## 已討論過的方向

### A. 再叫一次 LLM 驗證工具參數（提出過，評估後不建議當第一道）

缺點：

1. 同一個模型驗不出自己的抄寫錯誤；小模型逐字比對本來就弱。
2. 驗證本身也是生成，可能「修正」成第三個錯版本，或把對的改成錯的。
3. 每次工具呼叫多一輪 request，查核流程本來就要開好幾次分頁，成本與延遲都高。
4. 這件事不需要智慧——「這串字是不是原文裡的字」是純字串比對。

### B. 用程式碼驗（建議方向）

在工具真正執行前攔一道：

- 查核流程把原文存起來當「真值」。
- 模型要開搜尋時，取出網址裡的查詢字串，正規化（空白、全半形）後檢查
  是不是原文的子字串。
- **是** → 放行，用 `encodeURIComponent` 組網址。
- **不是** → 不問模型，程式直接修：在原文裡找編輯距離最近的片段替換；
  找不到就退回用原文本身（或原文前 N 字）去搜。
- 側邊欄顯示一行「已修正搜尋關鍵字：元紐土 → 混凝土」，讓使用者看得到發生過什麼。

原則：**模型只負責「決定查哪一段」（它擅長），字面由程式保證（它不擅長）。**

若仍想保留 LLM 驗證，讓它當第二道（程式判不出來的模糊情況），不要當第一道。

## 下一步

多測幾次，記錄樣本，看規律：

- 是否只有中文會壞？英文／數字／網址會不會也壞？
- 短字串 vs 長字串，出錯率差多少？
- 換模型（不同大小）出錯率差多少？→ 若只有小模型會壞，B 方案的價值更高。
- 除了 `open_new_tab`，`execute_javascript_on_page` 的 `code` 參數會不會也有類似失真？

樣本記錄區（原文 → 實際參數 → 模型 / 情境）：

- 羅馬混凝土 → 羅馬元紐土 → (待補模型名稱)
