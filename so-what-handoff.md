# 「所以呢？」功能 — Claude Code 交接文件

放在 barbara 專案根目錄，開工時讓 Claude Code 先讀這份。
搭配 `so-what-prompt.md`（LLM 行為定義）使用。

---

## 一、要做什麼

在 barbara 的右鍵選單新增第四個功能：

| 選單標籤 | 正式名稱 | 處理的環節 |
|---|---|---|
| 摘要 | 內容摘要 | 理解 —— 這段在說什麼 |
| 翻譯 | 語言轉換 | 取得 —— 讓我讀得懂 |
| 真的假的 | 事實查核 | 驗證 —— 這可信嗎 |
| **所以呢？** | **對話式知識內化** | **內化 —— 這對我意味著什麼** |

前三個都是對「文本」做事，一次請求一次回應，看完即丟。
第四個是對「使用者」做事，多輪對話，結束後產出一筆要留下的資料。

**這個差異是整個實作的主要難點，不是 prompt 換一換就好。**

---

## 二、開工第一步（先不要寫程式碼）

貼給 Claude Code 的第一則訊息：

> 我要在 barbara 新增一個右鍵選單功能，規格見 `so-what-handoff.md` 與 `so-what-prompt.md`。
>
> **先不要寫任何程式碼。** 請先做這四件事再回報：
>
> 1. 讀懂現有三個右鍵功能：context menu 註冊在哪、選取文字怎麼從 content script 傳到背景、LLM 呼叫怎麼包、結果 UI 顯示在哪（popup / sidepanel / content script 浮層）。
> 2. 這三個功能有沒有共用的抽象層？新功能應該接在哪一層？如果沒有共用層，說明各自重複了什麼。
> 3. 現有架構是「一次請求、一次回應」。要支援多輪對話，需要在哪裡維持狀態？背景 service worker 的生命週期會不會讓對話中途斷掉（MV3 的 service worker 會被回收）？
> 4. 專案目前有沒有任何持久化機制（storage / IndexedDB）？如果有，用在哪。
>
> 回報完再一起決定實作順序。

---

## 三、施工順序（重要）

### 第一版：對話跑得完，不碰儲存

**做**
- 右鍵選單新增「所以呢？」
- 選取文字後開啟對話 UI（沿用現有功能的顯示方式，不要另做一套）
- 第一輪的五個方向選項
- 多輪問答，選項用點的
- 收尾產出 takeaway，顯示在畫面上

**不做**
- 儲存卡片
- 卡片列表 / 搜尋 / 標籤 / 匯出
- 續談舊卡片
- 整頁模式

**理由**：對話流程的手感是唯一需要反覆調的東西，而它跟儲存完全無關。混在一起做，會分不清是「問題問得爛」還是「資料存錯了」。先讓自己用兩週，確認真的會用，再做儲存。

### 第二版：持久化

第一版跑順之後才做。卡片結構見第五節。

---

## 四、實作約束

**1. 提示詞放獨立檔案，不要寫死在程式裡。**
`so-what-prompt.md` 以檔案載入，程式只負責帶入變數與解析回傳。追問策略跟收尾判準之後一定會一直改，改文字檔比改程式碼快，非工程背景的人也改得動。

**2. LLM 回傳 JSON，不是自由文字。**
格式定義在 `so-what-prompt.md`。`phase: "ask"` 繼續問，`phase: "close"` 收尾。解析失敗時要有 fallback —— 不要讓一次格式錯誤把整段對話炸掉，退回顯示原始文字並允許使用者手動收尾。

**3. 每一輪都要有「夠了，存起來」的出口。**
這是防止使用者被問煩的關鍵。按了就立刻用當下已有的內容收尾。

**4. 對話狀態要能撐過 service worker 回收。**
MV3 的背景 service worker 閒置會被回收。對話進行到一半使用者去看別的東西再回來，狀態不能消失。狀態放哪由你評估，但這件事要在第一版就處理，不能留到之後。

**5. UI 沿用現有樣式。**
不要為這個功能另做一套視覺。它是選單裡的第四個功能，不是新產品。

---

## 五、卡片結構（第二版才用）

```yaml
id: card_20260904_a71f
type: internalization
created_at: 2026-09-04T14:22:11+08:00
title: 高速問題的判準是回流路徑，不是線長

# 雙來源：材料 + 認知
sources:
  - kind: webpage
    url: https://example.com/blog/reference-plane-integrity
    site_title: Signal Integrity Notes
    captured_at: 2026-09-04T14:22:10+08:00
    evidence_hash: sha256:3ab9…c17e   # 只雜湊引用到的證據句
  - kind: self
    via: 對話式知識內化

# 內化過程
direction: unknown          # 第一輪選的方向
initial_intent: null        # 一開始說不出來時為 null
final_takeaway: |
  我一直用線長在判斷高速問題，但真正的判準是回流路徑是否連續；
  去年教學板的 EMI 問題可能就是這個原因，值得回頭看平面層。
cognitive_shift: true       # 初始意圖與最終結論不一致

evidence_quotes:            # 只留佐證句，不存全文
  - 回流電流會沿著阻抗最低的路徑走，在高頻時即為訊號線正下方。
  - 跨越 3 mm 縫隙時 30–300 MHz 頻段輻射相差 12 dB。

open_questions:
  - 去年教學板的平面層長什麼樣？縫合電容當時加在哪？

thread_id: th_a71f          # 訊息本體放另一個 store，不內嵌
insight_summary: 跨越縫隙的容忍度取決於上升時間，而非時鐘頻率
tags: [EMI, 回流路徑, PCB]
```

### 四個容易做錯的地方

**`initial_intent` 與 `final_takeaway` 都要存。**
真正證明這個功能有用的不是最後那句話，而是這兩句之間的差距。相同時也是有用訊號：這段使用者早就清楚，下次類似的可以直接存、不用 grill。

**`thread_id` 而非內嵌 `messages`。**
對話會無限成長。內嵌會讓讀卡片列表時拖著全部訊息，效能難看，也違背「卡片保持精簡」的原則。

**`evidence_hash` 只雜湊引用到的證據句，不要雜湊整頁 HTML。**
廣告與時間戳每次都不同，雜湊整頁會天天誤報「來源已變更」。

**無來源的直接提問走同一套模型。**
使用者沒選文字、直接問的情況，一樣產出卡片，`sources` 只留 `kind: self`。維持單一資料模型，後續所有功能只需處理一種東西。

---

## 六、之後的階段（現在不做，但別擋住路）

依賴關係，不只是優先順序 —— 每一階段都需要前一階段累積的東西才有意義。

1. **卡片作為對話容器** —— 解決 barbara 現有的對話管理問題。對話散掉不是介面不好，是缺一個歸屬單位。有了卡片，對話就有標題、有錨點、有檢索入口，並可以三天後打開繼續問。
2. **整頁來源與拆卡** —— 正文萃取 + 分段處理，產出一張頁面卡與多張論點卡。`source.kind` 預留 `pdf` / `video`，進到卡片後長得一模一樣。
3. **搜尋與語意推薦** —— 不是加分項，是下一階段的前提。累積到兩三百張後手動勾選不可行。
4. **多卡合成** —— 四個算子：對比衝突（價值最高）、歸納共識、補齊缺口、收斂敘事。按鈕是捷徑，不取代自由輸入框。

**現在唯一要留的伏筆**：卡片的 id 與 `sources` 結構定下來就別再改。其他都可以之後再長。
